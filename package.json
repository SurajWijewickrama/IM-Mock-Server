{
  "name": "scene-control-server",
  "version": "1.0.0",
  "description": "Remote scene selector backend + dashboard for ImmersiveSmilePlus VR (SSE push to Unity).",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2"
  }
}
